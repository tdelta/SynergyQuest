For use of SVGs with React, save as "Optimized SVG" in Inkscape and set the following options:

* "Remove the XML declaration"
* "Remove metadata"
* "Remove comments"
* "Embed raster images"
* "Strip the xml:space attribute"
